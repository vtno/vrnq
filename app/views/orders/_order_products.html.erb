<% if item.amount > 0%>
  <div class="row">
    <div class="col-md-4"><%= item.product.name %></div>
    <div class="col-md-3"><%= "#{item.product.price} B" %></div>
    <div class="col-md-3"><%= "#{item.product.cost} B" %></div>
    <div class="col-md-2"><%= item.amount %></div>
  </div>
<% end %>
